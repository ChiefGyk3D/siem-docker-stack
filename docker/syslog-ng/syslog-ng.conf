# =============================================================================
# Syslog-ng — Centralized Syslog Receiver
# =============================================================================
# Receives syslog from pfSense, UniFi, and any RFC3164/5424 source.
#
# Architecture:
#   Devices → syslog-ng (UDP+TCP 514) → Local archive files
#                                       → Logstash (TCP 5045, JSON)
#
# pfSense syslog output targets this container's IP on port 514/UDP.
# Logs are:
#   1. Archived locally under /var/log/syslog-ng/ (rotated daily)
#   2. Forwarded as JSON to Logstash on TCP 5045 for OpenSearch indexing

@version: 4.2
@include "scl.conf"

# =============================================================================
# Global Options
# =============================================================================
options {
    chain_hostnames(off);
    flush_lines(0);
    use_dns(no);
    use_fqdn(no);
    dns_cache(no);
    owner("root");
    group("adm");
    perm(0640);
    stats(freq(0));
    keep_hostname(yes);
    create-dirs(yes);
};

# =============================================================================
# Sources — Receive from network
# =============================================================================
source s_network_udp {
    network(
        transport("udp")
        port(514)
    );
};

source s_network_tcp {
    network(
        transport("tcp")
        port(514)
    );
};

# =============================================================================
# Filters — Classify by source
# =============================================================================

# Match pfSense filterlog (firewall rule hits)
filter f_pfsense_filterlog {
    program("filterlog");
};

# Match pfSense (all other programs)
# Adjust the host() pattern to match your pfSense IP/hostname
filter f_pfsense {
    # Match by hostname or program patterns common to pfSense
    program("filterlog") or
    program("dhcpd") or
    program("unbound") or
    program("openvpn") or
    program("dpinger") or
    program("php-fpm") or
    program("check_reload_status") or
    program("sshd") or
    program("cron");
};

# Match UniFi devices (adjust hostname pattern as needed)
filter f_unifi {
    host("^(USW|USG|UAP|UDM)" flags(ignore-case));
};

# =============================================================================
# Destinations — Where logs go
# =============================================================================

# Local archival: daily rotated files
destination d_archive_pfsense {
    file("/var/log/syslog-ng/pfsense/${YEAR}-${MONTH}-${DAY}.log"
        create-dirs(yes)
    );
};

destination d_archive_unifi {
    file("/var/log/syslog-ng/unifi/${YEAR}-${MONTH}-${DAY}.log"
        create-dirs(yes)
    );
};

destination d_archive_general {
    file("/var/log/syslog-ng/general/${YEAR}-${MONTH}-${DAY}.log"
        create-dirs(yes)
    );
};

# Forward to Logstash as JSON over TCP 5045
destination d_logstash {
    network(
        "logstash"
        port(5045)
        transport("tcp")
        template("$(format-json --scope rfc5424 --scope nv-pairs --key ISODATE)\n")
    );
};

# =============================================================================
# Log Paths — Route messages
# =============================================================================

# pfSense → archive + Logstash
log {
    source(s_network_udp);
    source(s_network_tcp);
    filter(f_pfsense);
    destination(d_archive_pfsense);
    destination(d_logstash);
};

# UniFi → archive + Logstash
log {
    source(s_network_udp);
    source(s_network_tcp);
    filter(f_unifi);
    destination(d_archive_unifi);
    destination(d_logstash);
};

# Everything else → archive + Logstash
log {
    source(s_network_udp);
    source(s_network_tcp);
    destination(d_archive_general);
    destination(d_logstash);
    flags(fallback);
};
